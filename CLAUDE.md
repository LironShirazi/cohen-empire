# CLAUDE.md — הקשר לכל session פיתוח

קובץ זה נטען אוטומטית בתחילת כל session. תקרא אותו קודם כל דבר אחר — הוא
המפה למסמכי התכנון ולמערכת העיצוב, כדי שלא תצטרך "לגלות" את הפרויקט מחדש
בכל פעם.

## מה זה הפרויקט

**המירוץ למיליון — אימפריית כהן**: אפליקציית ווב Mobile-First, **בעברית בלבד, RTL מלא**,
לניהול המירוץ המשפחתי השנתי של משפחת כהן ביום העצמאות (מסורת 20+ שנה).
פירוט מלא: [README.md](README.md).

**מצב נוכחי: שלבים 0–2 הושלמו, שלב 3 בעבודה.** אפליקציית Next.js חיה
תחת `web/` (ראו §5), הסכמה וה-RPC-ים ב-`supabase/migrations/`. העץ המשפחתי
עבר ל-Supabase והוא כבר ראוט באפליקציה — ראו §4.

**שלב 2** ([docs/05-roadmap.md](docs/05-roadmap.md)) הושלם: צ'אט קבוצתי בזמן
אמת עם צירוף קבצים, אזכורים (@) והתראות In-App, הודעות רוחב מהמנהל (§7),
תור אישורי המשימות, מפת המנהל החיה (§8) והוספת משתתפים ידניים להרכב.
**החלוקה האוטומטית המאוזנת לקבוצות בוטלה** — הנימוק ב-docs/02 §3.6.

**שלב 3** ([docs/05](docs/05-roadmap.md)) בעבודה: העץ המשפחתי חובר ל-Supabase
(מיגרציה `0010`, §4), והגלריה עלתה (מיגרציה `0012`, §10). מה שנשאר בשלב —
משפטי סבא וסבתא, היכל התהילה, PWA והתאמת העיצוב של העץ למערכת העיצוב.

## 1. תמיד תתחיל מכאן — מסמכי התכנון (`docs/`)

לפני שכותבים קוד — לקרוא את אלה, בסדר הזה:

| מסמך | מה יש בו |
|---|---|
| [docs/01-requirements.md](docs/01-requirements.md) | דרישות מלאות: תפקידים/הרשאות, התחברות, ניהול מירוץ, חוויית משתתף, התראות, לידרבורד |
| [docs/02-architecture.md](docs/02-architecture.md) | הסטאק, תרשים ארכיטקטורה, כל ההחלטות המרכזיות (geofencing, RLS, קודים, אזכורים) — כולל מה שנשקל ובוטל |
| [docs/03-data-model.md](docs/03-data-model.md) | כל הטבלאות, שדות, יחסים, דוגמאות RLS ב-SQL |
| [docs/04-screens-ux.md](docs/04-screens-ux.md) | כל המסכים וזרימות המשתמש (תואם למה שמצויר ב-`claude-design/`) |
| [docs/05-roadmap.md](docs/05-roadmap.md) | חלוקה לשלבים 0–4. **תמיד תתחיל מהשלב הראשון שלא הושלם** |
| [docs/06-family-tree.md](docs/06-family-tree.md) | פיצ'ר העץ המשפחתי — מימוש קיים + תוכנית הסבה ל-Supabase |

**כלל ברזל:** אם משהו לא ברור מהקוד — התשובה כמעט תמיד נמצאת באחד מהמסמכים
האלה. אל תנחש התנהגות (למשל: לוגיקת לידרבורד, כללי RLS, שכבות ההתראות) —
תחפש אותה קודם ב-`docs/`.

## 2. הסטאק (מ-docs/02-architecture.md)

Next.js (React) + TypeScript · Tailwind CSS עם `dir="rtl"` · Supabase
(Postgres + Auth Google + Realtime + Storage + RLS) · Leaflet/OSM למפות ·
Browser Geolocation API · אירוח Vercel. אין שרת נפרד לתחזק.

## 3. מערכת העיצוב (`claude-design/`) — **חובה לפני בניית UI**

עוצב ע"י Claude (design system + מסכי UI). זה מקור האמת היחיד לעיצוב —
**אל תמציא צבעים/מרווחים/פונטים; תשתמש ב-tokens הקיימים.**

### `claude-design/design-system/`
- `styles.css` — **קובץ ה-tokens הראשי** (כל משתני ה-CSS: צבעים, רדיוסים,
  צללים, פונטים, קלאסים בסיסיים ל-btn/card/chip/field וכו'). כשמקימים
  Tailwind config — כל הצבעים/רדיוסים/צללים כאן צריכים להיכנס ל-`theme.extend`.
- `foundations/` — `brand.html`, `colors.html`, `typography.html`: הסבר חזותי
  על השפה העיצובית (Amazing-Race אדום+צהוב, navy קוסמי, זהב; פונטים Rubik
  לגוף + Secular One לכותרות).
- `components/` — רכיבי UI בודדים כ-HTML חי: buttons, inputs, team-card,
  chat, clue-card, clue-reveal, countdown, leaderboard, hall-of-fame,
  quote-card, walking-spinner, characters (סבא וסבתא), admin-participants.
  כל אחד = ה-reference הוויזואלי לרכיב המקביל ב-React.
- `assets/` ו-`uploads/` — תמונות/קריקטורות (סבא וסבתא, דגל צהוב, מצפן) לשימוש בפועל.

### `claude-design/ui-screens/`
- `המירוץ למיליון - סקיצות UI.dc.html` — סקיצות של כל המסכים (דף בית,
  זרימת כניסה בקודים, מסך משימה/רמז, צ'אט, לוח מובילים, מסכי ניהול תורן וכו'),
  בנוי ב-4 "סבבים" (`<section class="dv-turn" id="t1..t4">`). תואם למה שכתוב
  ב-`docs/04-screens-ux.md`.
- `family-tree/` — **עותק תואם** של המודול החי (זהה ל-`/family-tree` בשורש
  הפרויקט), מוטמע כאן ב-iframe בשני וריאנטים (מובייל/מחשב).
- `github.md` — יומן סנכרון בין ה-design tool למקור; מציג אילו מסכים כבר עוצבו
  ומתוך אילו חלקי docs.

**זרימת עבודה מומלצת בכל feature חדש:** לקרוא את חלק ה-UX הרלוונטי ב-`docs/04`,
לפתוח את המסך התואם ב-`claude-design/ui-screens/`, לבדוק את הרכיבים הרלוונטיים
ב-`claude-design/design-system/components/`, ואז לממש ב-React/Tailwind
תוך שימוש ב-tokens מ-`styles.css`.

## 4. העץ המשפחתי — ראוט באפליקציה עם מנוע vanilla (שלב 3)

```
web/app/family-tree/page.tsx        שער הכניסה: בלי משתמש מחובר אין עץ
web/app/family-tree/tree-canvas.tsx המעטפת — ה-DOM שהמנוע מחפש + הזרקת הלקוח
web/app/family-tree/family-tree.css העיצוב, כולו תחת `.ft-root`
web/public/family-tree/js/*.js      המנוע עצמו (store/layout/render/export/ui/main)
```

**עותק אחד בלבד.** עד שלב 3 היו שלושה (שורש הריפו, `claude-design/ui-screens/`,
ו-`web/public/`) עם סקריפט סנכרון ביניהם. עכשיו המקור היחיד הוא
`web/public/family-tree/js/` — הסקריפט והעותק בשורש נמחקו.

**למה המנוע נשאר vanilla:** `layout.js` (union-find, שיוך דורות, מרכוז)
ו-`render.js` (SVG + pan/zoom) עובדים, ו-React לא היה קונה להם כלום.
הם **לא נגעו** במעבר ל-Supabase — רק `store.js` הוחלף ו-`main.js` קיבל
`boot()` מפורש (ב-SPA האירוע `DOMContentLoaded` כבר קרה מזמן).

⚠️ **הקריאות ב-`store.js` חייבות להישאר סינכרוניות.** `render.draw()`
קורא ל-`all()` בתוך הרינדור, ו-ui.js משתמש באובייקט שחוזר מ-`addChild`/
`addPartner` מיד. לכן המתאם טוען הכל פעם אחת למפה בזיכרון, קורא ממנה,
וכותב אופטימית: מעדכן את המפה, מצייר, ושולח ל-Supabase ברקע (diff מול
תמונת מצב אחרונה). Realtime מיישר קו. מי שיהפוך `get`/`all` לאסינכרוניים
ישבור את שלושת הקבצים שבמפורש לא נגענו בהם.

⚠️ **ה-CSS כולו תחת `.ft-root`.** הקובץ נכתב כשהעץ היה עמוד עצמאי והגדיר
`:root` ו-`*` גלובליים — ובתוכם `--ink`, `--line`, `--gold`, `--danger`,
`--radius` ו-`--shadow`, שכולם שמות שכבר קיימים ב-`globals.css`. בלי
הסקופ הוא צובע מחדש את כל האפליקציה.

פירוט מלא — מודל הנתונים, ה-RLS ומה שנשאר פתוח: [docs/06-family-tree.md](docs/06-family-tree.md).

## 5. האפליקציה בפועל (`web/`)

Next.js 16 + App Router. **חשוב:** `web/AGENTS.md` מזהיר שגרסת ה-Next הזו
שונה ממה שמוכר — לקרוא את המדריך הרלוונטי ב-`node_modules/next/dist/docs/`
לפני שכותבים קוד (למשל `PageProps<'/route'>`, `params` כ-Promise,
`refresh()` מ-`next/cache`, ו-`proxy.ts` במקום `middleware.ts`).

```
web/lib/data.ts            שאילתות צד-שרת משותפות (מירוץ פעיל, הקבוצה שלי, תור אישורים)
web/lib/supabase/types.ts  טיפוסי ה-DB — לעדכן ידנית יחד עם כל מיגרציה
web/lib/geo.ts             Haversine למד המרחק בקליינט (השרת הוא הקובע)
web/app/join, /waiting     זרימת הכניסה: קוד משחק ← קוד קבוצה ← אישור מנהל
web/app/team, /team/play   מסך הקבוצה ומהלך המשחק
web/app/admin/[raceId]/…   לוח בקרה, קבוצות, תחנות (מפה), מהלך המירוץ החי
```

### כלל הזהב של שלב 1 — הרשאות ומרחק בשרת

כל שינוי מצב משחק עובר דרך פונקציית RPC ב-`supabase/migrations/0002_phase1_game.sql`
(`security definer` + בדיקת הרשאה בתוך הפונקציה), לא דרך כתיבה ישירה מהקליינט:

- `join_race` / `decide_join_request` — הצטרפות ואישור
- `get_team_state` — **מחזיר את המשימה רק אחרי שהשרת אימת הגעה.**
  `stations` בכלל לא קריאה למשתתפים, כדי ש-`task_content` לא ידלוף
- `arrive_at_station` — מחשב Haversine מול הרדיוס בשרת; `admin_open_station` לעקיפה
- `complete_station` — קורא את `completion_type` מהתחנה ומתעלם ממה שהקליינט טוען
- `get_leaderboard` — מחזיר **דירוג בלבד**, בלי ספירת משימות

לפני שמוסיפים פיצ'ר שנוגע במצב המשחק — לבדוק אם הוא שייך לאחת מהפונקציות
האלה, ולא לעקוף אותן עם כתיבה ישירה מהקליינט.

**אל תקפוץ לשלבים מאוחרים** לפני שהשלב הקודם עובד קצה־לקצה.

## 6. כללים כלליים לכל קוד שנכתב

- עברית בלבד בממשק, RTL מלא (`dir="rtl"`), Mobile-First תמיד קודם למחשב.
- טון עיצובי חם ומשפחתי — לא ממשק "משרדי" (ראו §3).
- לוגיקת אבטחה/הרשאות (מרחק GPS, RLS, יצירת התראות על אזכור) — **תמיד בצד שרת**,
  לא בקליינט בלבד. הפרטים המדויקים ב-[docs/02-architecture.md](docs/02-architecture.md) §3.
- אל תחשוף כמות משימות שהושלמו/נותרו בלידרבורד — רק דירוג (ראו docs/02 §3.3).
- לפני הוספת טבלה/שדה חדש — לבדוק אם הוא כבר מתועד ב-[docs/03-data-model.md](docs/03-data-model.md).

## 7. הצ'אט הקבוצתי (שלב 2)

בניגוד למצב המשחק, הצ'אט **לא** עובר דרך RPC (למעט הודעת רוחב, ראו
למטה): אין מה לאמת מעבר לזהות השולח וחברות בקבוצה, ושתיהן נאכפות
ב-RLS ב-`supabase/migrations/0005_phase2_chat.sql`
(`can_read_team_chat` — כולל מירוץ בארכיון, לקריאה; `can_post_team_chat` —
חבר קבוצה או מנהל תורן, ורק במירוץ שאינו בארכיון). אין מדיניות
UPDATE/DELETE — הודעה שנשלחה נשארת.

```
web/components/chat/chat-room.tsx   הצ'אט עצמו — Realtime + מחבר + העלאת קבצים
web/app/team/chat                   הצ'אט של המשתתף
web/app/admin/[raceId]/chat/…       אותו רכיב, מנקודת המבט של המנהל התורן
```

הקבצים עולים ל-bucket `chat-files` בתיקייה ששמה מזהה הקבוצה — אותה בדיקת
הרשאה כמו שליחת הודעה, ועם תקרת 50MB **ב-bucket עצמו** ולא רק במחבר.

### התראות In-App: אזכורים (@) והודעות רוחב (📣)

```
web/lib/mentions.ts                                  הטוקן, הסינון וההדגשה — לוגיקה טהורה
web/components/notifications/notification-center.tsx הבאנר הגלובלי (עוטף את layout)
web/components/notifications/unread-badge.tsx        הבאדג' — מרונדר בשרת
web/components/admin/broadcast-form.tsx              שליחת הודעת רוחב (מסך המירוץ החי)
supabase/migrations/0006_phase2_mentions.sql         הקשחת טריגר האזכורים + Realtime
supabase/migrations/0007_phase2_broadcast.sql        admin_broadcast()
```

שני סוגי ההתראה מצביעים על **הודעה בצ'אט**, ולכן הם משתמשים באותו באנר,
באותו באדג' ובאותה קפיצה לעוגן. הודעת רוחב אינה ישות חדשה: היא הודעת
צ'אט רגילה מהמנהל בכל קבוצת יעד (הצ'אט כבר מסמן אותה 📣) — docs/02 §3.8
קבע שאין מרכז התראות נפרד, הצ'אט הוא ההקשר.

**הטוקן הוא טקסט רגיל.** הבורר מכניס `@<שם מלא>` לגוף ההודעה ושומר את
המזהים ב-`mentioned_user_ids` בנפרד; השרת לא מנתח טקסט חופשי (docs/02 §3.8).
לפני שליחה המזהים נגזרים **מחדש מהטקסט הסופי** — אם מחקו את שם האזכור,
לא תישלח התראה על משהו שלא כתוב בהודעה.

**אין ולא יהיה INSERT מהקליינט על `notifications`** — הקליינט רשאי רק
לקרוא את שלו ולסמן `read_at`. לכן אזכור נוצר בטריגר `handle_message_mentions`
(ורק למי שבאמת בצ'אט הזה), והודעת רוחב עוברת דרך `admin_broadcast()` —
זו הסיבה היחידה ששליחה מהמנהל היא RPC בעוד ששאר הצ'אט כותב ישירות מול RLS.

שלוש שכבות תצוגה (docs/02 §3.8), ובכוונה בשלושה מקומות שונים בקוד:
באנר קופץ ב-`NotificationCenter` שעוטף את כל האפליקציה ב-`layout.tsx`;
באדג' שמרונדר **בשרת** ב-`/team` וב-`/admin/[raceId]/chat` (`@` גובר על 📣);
וקפיצה להודעה (עוגן `#m-<id>`) בפתיחת הצ'אט. הצ'אט הפתוח מדווח על עצמו
דרך `useActiveChat`, כדי שלא יקפוץ באנר על הודעה שכבר על המסך.

## 8. מפת המנהל החיה (שלב 2)

```
supabase/migrations/0008_phase2_live_map.sql  team_locations + report_team_location
web/components/game/location-reporter.tsx     דיווח מהמשתתף (מסך המשחק)
web/components/admin/live-map.tsx             המפה עצמה (Leaflet)
```

**הכלל שאסור לשבור:** `team_locations` היא **תצוגה בלבד**. פתיחת משימה
נשענת אך ורק על הקואורדינטות שנשלחות ל-`arrive_at_station` ומאומתות שם
מול הרדיוס (docs/02 §3.1). מי שיחשב מרחק מהטבלה הזו עוקף את האימות —
כאן נשמר מה שהמכשיר דיווח, בלי הצלבה מול שום דבר.

שורה אחת לקבוצה, לא שובל (הנימוק ב-docs/02 §3.9). הקריאה היא למנהל
התורן בלבד ב-RLS — משתתף שרואה איפה כולם הורג את המתח שהלידרבורד נבנה
במיוחד כדי לשמור (docs/02 §3.3).

`LocationReporter` מריץ `watchPosition` משלו ולא מתחלק עם `DistanceMeter`
בכוונה: מד המרחק קיים רק במצב "בדרך לתחנה", והדיווח צריך לרוץ בכל מצב —
גם כשהקבוצה עומדת בתחנה, כי בדיוק אז המנהל שואל איפה הם.

## 9. מלכודות ידועות ומה עוד פתוח

### שני מקורות לאותה הודעה בצ'אט

הרשימה מתמלאת משני מקומות עם פורמט זמן שונה — PostgREST מחזיר ISO
(`2026-08-11T20:00:00+00:00`) ו-Realtime מוסר את השורה כפי שהיא ב-WAL
(`2026-08-11 20:00:00+00`). `toIsoTimestamp` מיישר את שורת ה-Realtime
בנקודת הכניסה היחידה שלה; בלי זה המיון מתבלבל בין המקורות ו-`new Date()`
מחזיר Invalid Date בחלק מהדפדפנים. כל מקור חדש להודעות צריך לעבור דרך
`append` (דדופ לפי `id`) ולא ל-`setMessages` ישירות.

### חובות פתוחים בצ'אט

- **קבצים יתומים:** קובץ שהועלה ולא נשלח (או הוחלף במחבר) נשאר ב-bucket
  לנצח, ולחברי הקבוצה אין מדיניות DELETE שתנקה אותו — רק למנהל התורן.
  צריך החלטת שימור אמיתית (ניקוי תקופתי? מחיקה בהחלפה?), לא תיקון נקודתי.
- **קריאה מארכיון עובדת רק למנהלים:** `can_read_team_chat` מרשה למשתתף
  לקרוא צ'אט של מירוץ בארכיון, אבל `getMyMembership` מסננת ל-live/open/finished
  ולכן הוא בכלל לא מגיע ל-`/team/chat` שם. אם רוצים שהמשפחה תוכל לקרוא
  צ'אטים ישנים — זו הנקודה לשנות, ולא ה-RLS.

## 10. הגלריה המשפחתית (שלב 3)

```
supabase/migrations/0012_phase3_gallery.sql         פתיחת gallery_photos לכתיבה + bucket
supabase/migrations/0013_phase3_gallery_albums.sql  המעבר לאלבומים (מבטל את הסידור לפי שנה)
web/app/gallery/page.tsx                            רשימת האלבומים
web/app/gallery/[albumId]/page.tsx                  אלבום
web/components/gallery/new-album-form.tsx           פתיחת אלבום
web/components/gallery/album-screen.tsx             שם, העלאה ורשת המדיה
web/components/gallery/photo-lightbox.tsx           תצוגה מוגדלת, כותרת ומחיקה
web/lib/image.ts                                    הקטנת תמונות ל-1600px לפני העלאה
```

**היחידה היא אלבום, לא שנה.** 0012 סידר את הגלריה לפי שנת המירוץ; 0013
ביטל את זה. הגלריה היא של **המשפחה** לכל שימוש — חתונה, טיול, מירוץ —
ולכן אין בה שנה, אין `race_id` בשימוש, ואין למנהל תורן מעמד מיוחד
(אלבום של חתונה לא שייך לשום מירוץ).

**האלבום שייך למשפחה ולא לפותח אותו** — כמו עלה בעץ (docs/06 §4): כל בן
משפחה מוסיף מדיה לכל אלבום ומתקן שם. מה שכן שמור לפותח ולמנהל-על היא
**מחיקת האלבום**, והיא נכשלת כל עוד יש בו מדיה (`on delete restrict`,
שגיאה 23503 שהמסך מתרגם) — בדיוק כמו מחיקת עלה עם צאצאים.

⚠️ **הרשאה ברמת העמודה, לא רק RLS.** ל-`authenticated` יש
`grant update (name, updated_at)` על האלבומים ו-`grant update (caption,
album_id)` על המדיה. RLS מחליט על אילו *שורות* מותר לכתוב, לא על אילו
עמודות — ובלי זה "תיקון שם" היה יכול להיות גם החלפת `created_by`,
ותיקון כותרת היה יכול להחליף את `url`.

**סדר המחיקה: קודם השורה, אחר-כך הקובץ.** השורה היא מה שהמסך קורא — אם
נמחק קודם הקובץ ומחיקת השורה תיכשל, תישאר תמונה שבורה על המסך. הכיוון
הזה משאיר לכל היותר קובץ יתום ב-bucket, אותו חוב שכבר קיים ב-`chat-files`
(§9): מדיניות ה-Storage מרשה מחיקת קובץ רק לבעליו ולמנהל-על.

**סרטונים לא עוברים הקטנה** — `lib/image.ts` הוא לתמונות בלבד, ולכן
תקרת ה-bucket היא 50MB כמו ב-`chat-files`. הסוג נגזר מהסיומת דרך
`lib/media.ts` ולא נשמר בשורה.
